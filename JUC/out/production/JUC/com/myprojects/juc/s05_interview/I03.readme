面试题：要求线程顺序打印A1 B2 C3...Z26
1.WaitNotify:声明打印数字和打印字母的方法，在打印数字时让线程处于wait状态，打印字母时打印一个字母然后唤醒打印数字的线程，两个方法互相调用
2.CountDownLatch(有问题):参数传入1，先让打印数字线程阻塞，再让打印字母线程count--唤醒.虽说都互相调用方法，但是线程不是顺序执行的
3.State:AQS类中有这个类似变量。state代表可以执行的方法，1代表打印字母方法可以运行，0代表打印数字方法可以运行，此方法比WaitNotify效率高很多